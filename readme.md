#### git基础操作
git源代码版本管理工具
```bash
git init    # 创建一个代码仓库
git add     # 添加文件到缓存区
git commit  # 提交代码到本地仓库
                # . 提交所有的文件
                # 文件名 提交指定的文件
git status  # 查看当前代码仓库的状态
git log     # 查看日志
git reflog  # 查看所有的日志
git reset   # 版本回退
```